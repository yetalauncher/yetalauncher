export enum Pages {
    Home,
    Create,
    Accounts,
    Settings
}

export enum SlInstanceType {
    CurseForge,
    MultiMC
}

export enum SlNotifState {
    Running,
    Success,
    Warning,
    Error
}